# =============================================================================
# .env.local SOBRESCRIBE a .env en Vite.
# Renómbralo a .env.local SOLO cuando compiles el EXE local.
# Antes de compilar APK, elimínalo o renómbralo para que no interfiera.
# =============================================================================
VITE_API_BASE_URL=http://localhost:8000/api/v1
